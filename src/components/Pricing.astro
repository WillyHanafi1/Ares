---
const plans = [
  {
    name: 'Starter',
    price: 'Rp 3 Juta',
    period: '/proyek',
    description: 'Cocok untuk bisnis kecil yang ingin memulai otomatisasi',
    features: [
      '1 Workflow Automation',
      'Setup & Konfigurasi',
      'Training Penggunaan',
      'Support 7 Hari',
      '🎁 BONUS: Landing Page Gratis',
    ],
    highlight: false,
    cta: 'Mulai Sekarang',
  },
  {
    name: 'Business',
    price: 'Rp 5-7 Juta',
    period: '/proyek',
    description: 'Solusi lengkap untuk bisnis yang ingin scale up',
    features: [
      'AI Chatbot WhatsApp/IG',
      '3 Workflow Automations',
      'CRM Integration',
      'Training Tim',
      'Support 14 Hari',
      'Revisi 2x',
      '🎁 BONUS: Landing Page Gratis',
    ],
    highlight: true,
    cta: 'Pilih Paket Ini',
  },
  {
    name: 'Enterprise',
    price: 'Rp 10 Juta+',
    period: '/proyek',
    description: 'Solusi custom untuk kebutuhan bisnis kompleks',
    features: [
      'Custom AI Solutions',
      'Unlimited Workflows',
      'Full System Integration',
      'Dedicated Support',
      'Training Intensif',
      'Maintenance 30 Hari',
      'Priority Support',
      '🎁 BONUS: Website Profesional Gratis',
    ],
    highlight: false,
    cta: 'Hubungi Kami',
  },
];

const benefits = [
  { icon: 'payments', text: 'Bayar di Akhir Project', desc: 'Setelah Anda puas dengan hasilnya' },
  { icon: 'language', text: 'Landing Page Gratis', desc: 'Untuk semua paket' },
  { icon: 'verified', text: 'Garansi Kepuasan', desc: 'Money back jika tidak sesuai' },
];
---

<section id="pricing" class="py-24 relative">
  <div class="mx-auto max-w-7xl px-6 lg:px-8">
    <!-- Header -->
    <div class="text-center mb-12">
      <span class="animate-on-scroll animate-blur-in mb-2 inline-block text-sm font-semibold uppercase tracking-wider text-primary">Harga Transparan</span>
      <h2 class="animate-on-scroll animate-from-bottom animate-delay-100 text-3xl font-bold tracking-tight text-white md:text-4xl mb-4">
        Investasi untuk Pertumbuhan Bisnis
      </h2>
      <p class="animate-on-scroll animate-from-bottom animate-delay-200 text-gray-400 max-w-2xl mx-auto">
        Pilih paket yang sesuai dengan kebutuhan bisnis Anda. Semua harga sudah termasuk konsultasi awal gratis.
      </p>
    </div>
    
    <!-- Benefits Banner -->
    <div class="animate-on-scroll animate-scale mb-12">
      <div class="glass-panel rounded-2xl p-6 md:p-8">
        <div class="grid gap-6 md:grid-cols-3">
          {benefits.map((benefit) => (
            <div class="flex items-center gap-4">
              <div class="flex-shrink-0 w-12 h-12 rounded-xl bg-primary/20 flex items-center justify-center">
                <span class="material-symbols-outlined text-primary">{benefit.icon}</span>
              </div>
              <div>
                <h4 class="text-white font-bold">{benefit.text}</h4>
                <p class="text-gray-400 text-sm">{benefit.desc}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
    
    <!-- Pricing Cards -->
    <div class="grid gap-8 md:grid-cols-3">
      {plans.map((plan, index) => (
        <div class={`animate-on-scroll ${index === 0 ? 'animate-from-left' : index === 2 ? 'animate-from-right' : 'animate-scale'} animate-delay-${(index + 1) * 100} relative group`}>
          <!-- Highlight glow for featured plan -->
          {plan.highlight && (
            <div class="absolute -inset-[1px] bg-gradient-to-r from-primary to-secondary rounded-2xl blur-sm opacity-50 group-hover:opacity-75 transition-opacity"></div>
          )}
          
          <div class={`relative h-full rounded-2xl p-8 ${plan.highlight ? 'bg-[#13132B] border-0' : 'glass-panel'} flex flex-col`}>
            <!-- Popular Badge -->
            {plan.highlight && (
              <div class="absolute -top-4 left-1/2 -translate-x-1/2">
                <span class="bg-gradient-to-r from-primary to-secondary text-white text-xs font-bold px-4 py-1.5 rounded-full">
                  PALING POPULER
                </span>
              </div>
            )}
            
            <!-- Plan Header -->
            <div class="text-center mb-6">
              <h3 class="text-xl font-bold text-white mb-2">{plan.name}</h3>
              <div class="flex items-baseline justify-center gap-1">
                <span class={`text-4xl font-black ${plan.highlight ? 'text-gradient-primary' : 'text-white'}`}>{plan.price}</span>
                <span class="text-gray-400 text-sm">{plan.period}</span>
              </div>
              <p class="text-gray-400 text-sm mt-3">{plan.description}</p>
            </div>
            
            <!-- Divider -->
            <div class="h-px bg-white/10 my-6"></div>
            
            <!-- Features -->
            <ul class="space-y-3 mb-8 flex-grow">
              {plan.features.map((feature) => (
                <li class={`flex items-center gap-3 text-sm ${feature.startsWith('🎁') ? 'text-green-400 font-semibold' : 'text-gray-300'}`}>
                  {!feature.startsWith('🎁') && (
                    <span class={`material-symbols-outlined text-[18px] ${plan.highlight ? 'text-primary' : 'text-green-400'}`}>check_circle</span>
                  )}
                  {feature}
                </li>
              ))}
            </ul>
            
            <!-- CTA Button -->
            <a 
              href="#kontak"
              class={`w-full flex items-center justify-center py-3 px-6 rounded-lg font-bold text-sm transition-all
                ${plan.highlight 
                  ? 'bg-gradient-to-r from-primary to-secondary text-white hover:shadow-[0_0_30px_rgba(19,19,236,0.4)] hover:scale-105' 
                  : 'bg-white/5 border border-white/10 text-white hover:bg-white/10 hover:border-white/20'
                }`}
            >
              {plan.cta}
            </a>
          </div>
        </div>
      ))}
    </div>
    
    <!-- Trust Note -->
    <div class="animate-on-scroll animate-from-bottom animate-delay-400 mt-12 text-center space-y-2">
      <p class="text-gray-400">
        💰 <span class="text-white font-semibold">Bayar Setelah Project Selesai</span> — Tidak ada DP, bayar setelah Anda puas dengan hasilnya
      </p>
      <p class="text-gray-500 text-sm">
        💡 Belum yakin? Konsultasi awal <span class="text-primary font-semibold">GRATIS</span> tanpa komitmen.
      </p>
    </div>
  </div>
</section>

<style>
  .text-gradient-primary {
    background: linear-gradient(to right, #1313ec, #7c3aed);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
</style>
